DATABASE_URL=postgres://workorder:workorder@127.0.0.1:5432/workorder
SESSION_SECRET=replace-with-a-random-secret-of-at-least-32-characters
HOST=127.0.0.1
PORT=3000
NODE_ENV=development
# Public origin used in Stripe return links, e.g. https://workorder.example.com
APP_URL=http://127.0.0.1:3000
# Stripe Connect (test mode). Leave blank to disable online funding.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Live keys are refused unless this is exactly true.
PAYMENTS_LIVE=false
